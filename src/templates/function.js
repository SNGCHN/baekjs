const solution = (input) => {
  // 한 단어: const word = input.trim();
  // 한 줄 (공백 구분): const [a, b] = input.trim().split(/\s+/).map(Number);
  // 여러 줄: const lines = input.trim().split(/\r?\n/);
  // 주석 제거는 내 정보 > 설정에서 변경할 수 있습니다.
};
